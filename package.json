{
  "name": "kurier-app",
  "version": "0.1.0",
  "description": "Courier delivery helper PWA — OCR addresses, match parcels + mail, offline route optimization",
  "type": "module",
  "engines": {
    "node": ">=20.0.0 <21",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-extractor.mjs && node scripts/test-geocode.mjs && node scripts/test-route.mjs && node scripts/test-ors-proxy.mjs && node scripts/test-db.mjs && node scripts/test-migrate.mjs"
  },
  "dependencies": {
    "idb": "8.0.0",
    "tesseract.js": "5.1.1"
  },
  "devDependencies": {
    "fake-indexeddb": "6.0.0",
    "vite": "5.4.10",
    "vite-plugin-pwa": "0.20.5"
  }
}
